<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:windowSoftInputMode="adjustResize"
        android:theme="@style/Theme.MedicalManagementSystem">

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".PatientHomeActivity" />
        <activity android:name=".BookAppointmentActivity" />
        <activity android:name=".BillsHistoryActivity" />
        <activity android:name=".TreatmentHistoryActivity" />
        <activity android:name=".DoctorHomeActivity" />
        <activity android:name=".PendingAppointmentsActivity" />
        <activity android:name=".TodaysAppointmentsActivity" />
        <activity android:name=".HistoryUpdateActivity" />
        <activity android:name=".GenerateBillActivity" />
        <activity android:name=".PatientHistoryActivity" />

    </application>

</manifest>
